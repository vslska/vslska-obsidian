## Что такое `hyprpm`?

`hyprpm` (**Hyprland Plugin Manager**) — официальный, встроенный в Hyprland (с версии **0.40.0+**) менеджер **динамических плагинов** (`.so`-библиотек), позволяющий:

- Устанавливать плагины из Git-репозиториев  
- Автоматически собирать их через `make`  
- Загружать в runtime *без перезапуска* сессии  
- Управлять зависимостями (если указаны в `hyprpm.toml`)  
- Обеспечивать **изоляцию**: каждый плагин живёт в `~/.local/share/hyprpm/modules/`

Это — не пакетный менеджер ОС.  
Это — **механизм расширения ядра WM**, как `DKMS` для ядра Linux.


----

## Установка (если ещё не в системе)

`hyprpm` поставляется **в комплекте с Hyprland ≥0.40**, но может быть отключён в некоторых сборках.
Можно проверить:
```bash
hyprpm --help

# Или:

ls $(dirname $(which hyprland))/hyprpm
```
	Если отсутствует — пересобрать Hyprland с `enableHyprpm = true` (в Nix) или из исходников с флагом `-Dhyprpm=enabled`.

---
## Быстрый старт: установка `hyprmodoro`

```bash
# 1. Добавить репозиторий плагина (клонирует в ~/.local/share/hyprpm/sources/)
hyprpm add https://github.com/0xFMD/hyprmodoro

# 2. Собрать и включить
hyprpm enable hyprmodoro

# 3. Загрузить *сейчас* (без перезапуска Hyprland!)
hyprctl plugin load hyprmodoro

#  Готово. Плагин будет автоматически загружаться при старте.
```

Чтобы обновить:
```bash
hyprpm update hyprmodoro && hyprpm enable hyprmodoro
```

---
## Структура каталогов
```bash
~/.local/share/hyprpm/
├── sources/               # Клонированные репозитории (git)
│   └── hyprmodoro/        → содержит Makefile, src/, hyprpm.toml
├── modules/               # Собранные .so-файлы
│   └── hyprmodoro.so      → результат `make all`
├── config.toml            # Глобальные настройки hyprpm (редко трогать)
└── enabled/               # Симлинки → только «включённые» модули
    └── hyprmodoro.so → ../modules/hyprmodoro.so
```
	Плагин загружается **только если есть симлинк в `enabled/`** → как `systemctl enable`.

## `hyprpm.toml` — метаданные плагина
Если  писать плагин, или хотеть понять, откуда берутся зависимости — необходимо смотреть  корне репозитория:
```bash
# Пример для hyprmodoro (упрощённо)
[plugin]
name = "hyprmodoro"
description = "Pomodoro timer for Hyprland"
author = "0xFMD"
version = "0.2.1"

[build]
make_target = "all"
output = "hyprmodoro.so"

[dependencies]
# Пусто — потому что он чистый C++ + Hyprland headers
# Но могло бы быть:
# headers = ["hyprland-0.42.0"]
```
	Важно: плагин **должен собираться под ту же версию Hyprland**, что и запущена.  
	Иначе — `symbol lookup error` (как `kernel-headers mismatch`).

## Основные команды `hyprpm`
| Комманда                | Описание                                               |
| ----------------------- | ------------------------------------------------------ |
| `hyprpm add <url>`      | Клонирует репозиторий в `sources/`                     |
| `hyprpm remove <name>`  | Удаляет источник и модуль                              |
| `hyprpm build <name>`   | Пересобирает `.so` (без включения)                     |
| `hyprpm enable <name>`  | Собирает + создаёт симлинк в `enabled/`                |
| `hyprpm disable <name>` | Удаляет симлинк → плагин не грузится                   |
| `hyprpm list`           | Показывает: `installed`, `enabled`                     |
| `hyprpm update <name>`  | `git pull` + `build` (но не `enable` — нужно повторно) |
	Совет: после `hyprpm update`, всегда делай `hyprpm enable`, чтобы пересобрать под текущую версию.

## Ручная загрузка (для тестов)
Даже если плагин _не включён_, можно загрузить `.so` напрямую:
```bash
hyprctl plugin load ~/.local/share/hyprpm/modules/hyprmodoro.so
```
→ Полезно для отладки своей сборки.  
→ Работает _мгновенно_ — без `MOD+SHIFT+R`.

## Настройка плагинов — в `hyprland.conf`

Все параметры — через `plugin:<name>:<key>`.  
Пример для `hyprmodoro`:
```ini
plugin:hyprmodoro:enabled = true
plugin:hyprmodoro:work_duration = 25
plugin:hyprmodoro:rest_duration = 5
plugin:hyprmodoro:border:color = rgba(a6e3a1cc)  # Catpuccin Green
plugin:hyprmodoro:text:font = JetBrainsMono Nerd Font
plugin:hyprmodoro:text:work_prefix = "🐉 "
```
	Изменения вступают в силу **сразу** — не нужно перезагружать WM.

## Управление через `hyprctl` (runtime)
Плагины предоставляют свои _dispatch-команды_ и _получение состояния_:
```bash
# Управление
hyprctl dispatch hyprmodoro:start
hyprctl dispatch hyprmodoro:stop
hyprctl dispatch hyprmodoro:skip

# Интроспекция
hyprctl hyprmodoro:getState    # → work / rest / idle
hyprctl hyprmodoro:getTime     # → 123 (секунд)
hyprctl hyprmodoro:getProgress # → 0.42 (float)
```
Можно использовать в скриптах, виджетах (`hyprpanel`), `cron`, `systemd --user`.

## Другие популярные плагины для `hyprpm`
| Название                                             | Описание                                                        |
| ---------------------------------------------------- | --------------------------------------------------------------- |
| [`hyprpicker`](https://github.com/hyprwm/hyprpicker) | Выбор цвета (родной, от Hyprwm)                                 |
| [`hyprgrass`](https://github.com/hyprwm/hyprgrass)   | Эффект размытия (вместо `blur`)                                 |
| [`hyprtrails`](https://github.com/0xFMD/hyprtrails)  | Визуальные следы за курсором/окнами                             |
| [`hyprlock`](https://github.com/hyprwm/hyprlock)     | Экран блокировки (не плагин, но устанавливается через `hyprpm`) |
| [`hyprshot`](https://github.com/hyprwm/hyprshot)     | Скриншоты (native Wayland)                                      |
	Искать в:  
    https://github.com/hyprwm

## Ограничения и предостережения
-  Плагин **должен компилироваться под текущую версию Hyprland**  
    → Обновил Hyprland? Сделай `hyprpm update && hyprpm enable` для всех плагинов.
- `.so` грузится в _процесс Hyprland_ → краш плагина = краш сессии.  
    → Тестируй новые плагины в отдельной сессии (`hyprland --config /tmp/test.conf`).
- Нет sandbox’а (пока) → плагин имеет полный доступ к Hyprland API.


