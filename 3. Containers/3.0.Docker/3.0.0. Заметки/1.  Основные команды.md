
*"Readable code is poetry — and Dockerfiles are its sonnets."*  
— vslsk, 2025

---
##  1. Контейнеры

| Команда                            | Описание                                                                                 |
| ---------------------------------- | ---------------------------------------------------------------------------------------- |
| `docker run [OPTIONS] IMAGE [CMD]` | Запустить **новый** контейнер из образа                                                  |
| `docker ps`                        | Список **запущенных** контейнеров                                                        |
| `docker ps -a`                     | Список **всех** контейнеров (включая остановленные)                                      |
| `docker stop <container>`          | Остановить контейнер (*gracefully*, через `SIGTERM`)                                     |
| `docker kill <container>`          | Принудительно завершить (*`SIGKILL`*)                                                    |
| `docker start <container>`         | Запустить ранее остановленный контейнер                                                  |
| `docker restart <container>`       | Перезапустить контейнер                                                                  |
| `docker rm <container>`            | Удалить **остановленный** контейнер<br>→ `docker rm -f <name>` — удалить даже запущенный |
| `docker exec -it <container> bash` | Подключиться к **запущенному** контейнеру (интерактивно)                                 |
| `docker logs <container>`          | Показать логи контейнера<br>→ `docker logs -f` — с «хвостом» (как `tail -f`)             |
| `docker inspect <container>`       | Подробная информация в JSON (полезно для дебага сети, путей, env)                        |
|                                    |                                                                                          |
### 🛠 Часто используемые флаги `docker run`
```bash
-it          # интерактив + TTY (для терминала)
-d           # detached mode (в фоне)
--rm         # удалить контейнер после остановки
-p <h>:<c>   # проброс портов: хост:контейнер (например, -p 8080:80)
-v <src>:<dst> # монтирование тома: хост → контейнер
--name <str> # задать человекочитаемое имя
--env KEY=VAL # передать переменную окружения
```

```bash
docker run -d --name web -p 8080:80 -v ./html:/usr/share/nginx/html nginx
```

## 2. Образы
| Команда                          | Описание                                                         |
| -------------------------------- | ---------------------------------------------------------------- |
| `docker images`                  | Список локальных образов                                         |
| `docker pull <image>[:<tag>]`    | Скачать образ (по умолчанию `:latest`, но лучше указывать явно!) |
| `docker build -t <name>:<tag> .` | Собрать образ из `Dockerfile` в текущей директории               |
| `docker rmi <image>`             | Удалить образ (по имени/ID)<br>→ `docker rmi -f` — принудительно |
| `docker tag <src> <target>`      | Присвоить новый тег (например, `myapp:dev` → `myapp:prod`)       |
| `docker push <image>:<tag>`      | Отправить в реестр (требуется `docker login`)                    |

> [!NOTE] Совет
>  Всегда тегировать образы осмысленно:
>  - `app:2025-11-24-dev`
>  - `nginx:stable-alpine`
>  - `postgres:16`
> 	 Избегать `:latest` в production - он неявный и непредсказуемый>

## 3. Система и очистка:
| Команда                                | Описание                                                                                                                                                                        |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docker system prune`                  | Удалить всё «мертвое»: остановленные контейнеры, неиспользуемые сети, образы без тегов, кэш сборки<br>→ `-a` — также удалить **все** неиспользуемые образы (не только dangling) |
| `docker system df`                     | Дисковое пространство: сколько занято образами, контейнерами, томами, кэшем                                                                                                     |
| `docker volume ls` / `prune` / `rm`    | Управление постоянными томами                                                                                                                                                   |
| `docker network ls` / `inspect` / `rm` | Просмотр и настройка сетей (`bridge`, `host`, `none`, custom)                                                                                                                   |
## 4. Быстрые "Лайфхаки"

```bash
# Последний запущенный контейнер → удобно для exec/logs
docker exec -it $(docker ps -q -l) bash

# Удалить ВСЕ остановленные контейнеры
docker rm $(docker ps -aq -f status=exited)

# Удалить все dangling образы (без тегов и неиспользуемые)
docker rmi $(docker images -q -f dangling=true)

# Запустить контейнер и сразу в него — one-liner
docker run -it --rm alpine sh
```

## 5. Дополнительно

| Образ      | Пример запуска                                                    |
| ---------- | ----------------------------------------------------------------- |
| `alpine`   | `docker run -it --rm alpine sh` (минимализм)                      |
| `nginx`    | `docker run -d -p 8080:80 --name web nginx`                       |
| `redis`    | `docker run -d --name redis -p 6379:6379 redis:alpine`            |
| `postgres` | `docker run -d --name db -e POSTGRES_PASSWORD=secret postgres:16` |
